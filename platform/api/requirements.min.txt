# =============================================================================
# requirements.min.txt — Build rapid pentru development
# Exclude dependențe grele (LangGraph, docling, LanceDB) care nu sunt active încă
# Folosit automat în docker-compose.dev.yml prin ARG BUILD_ENV=dev
# =============================================================================

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.8.2
pydantic-settings==2.4.0
email-validator==2.2.0

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Redis
redis[asyncio]==5.0.8

# HTTP client
httpx==0.27.2

# Logging
structlog==24.4.0

# Rate limiting
slowapi==0.1.9

# Form / file upload
python-multipart==0.0.12

# bcrypt 4.x incompatibil cu passlib 1.7.4 (detect_wrap_bug)
bcrypt==3.2.2
